replicaCount: 1

image:
  repository: devops-homework
  tag: "1234"

service:
  port: 80
  targetPort: 8000

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

  healthCheckPath: /health

ingress:
  enabled: false
  host: myapp.local

environment: dev